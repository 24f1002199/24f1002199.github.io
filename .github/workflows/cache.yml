name: cache-workflow

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/test
          key: cache-28f114f

      - name: prime-cache-28f114f
        run: |
          echo "Cache hit: ${{ steps.cache.outputs.cache-hit }}"
